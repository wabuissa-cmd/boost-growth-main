# Railway — Dockerfile deploy
# IMPORTANT: Settings → Deploy → Start Command must be EMPTY (Dockerfile CMD runs uvicorn).
# Delete any "npx serve ..." or manual uvicorn command in Railway UI.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 180
