# Railway — simple Docker (Python only, frontend pre-built in backend/static)
# Root Directory MUST be empty (not "frontend")

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/"
healthcheckTimeout = 120
